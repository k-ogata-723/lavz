<li id="micropost-<%= micropost.id %>">
  <!-- edit_micropost.js.jsxからselectedMicropostIdを取り出すためにjsxを利用 -->
  <!-- <script type="text/jsx">
     var HelloWorld = React.createClass({
       render: function() {
         return (
           <p>Hello!World!</p>
         );
       }
     }); -->
  <button class="btn-old-micropost view-protocol" id="selectedMicropostId" selected_id="<%= micropost.id %>" >
      <h2 class="title" style="margin: 0px"><%= micropost.content %></h2>
      <h2 class="messageBox" style="margin: 0px">
        <%= User.feed_protocol(micropost.id) %>
      </h2>
        <%= image_tag micropost.picture.url if micropost.picture? %>
    <span class="timestamp">
      Posted <%= time_ago_in_words(micropost.created_at) %> ago.
      <% if current_user?(micropost.user) %>
        <% link_to "delete", micropost, method: :delete, data: { comfirm: "You sure?" } %>
      <% end %>
    </span>
  </button>
</li>
